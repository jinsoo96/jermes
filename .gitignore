__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/
.venv/
venv/

# 로컬 실행 흔적 — 기계마다 다르고 레포에 들어갈 이유가 없다
*.log
.env
.jermes-test/
.jermes/

# 배포 산출물 — 태그마다 다시 만든다. 레포에 넣을 이유가 없다.
dist/
build/

# 그때그때 만들어 돌려보는 py — 검증 도구(smoke.py·audit_*.py·absorb.py·demo.py)와
# 다르다. 그건 README 에 적힌 절차의 일부라 남기고, 아래는 남기지 않는다.
tmp_*.py
scratch_*.py
probe_*.py
check_*.py
try_*.py
demo_llm.py
demo_weak_sweep.py
watch_competition.py

# 그때그때 돌려보는 자리(스텁 서버·평가 스크립트·실험 홈).
# C 임시폴더 대신 여기 둔다 - 이 기계는 C 가 늘 빠듯하다.
.scratch/
